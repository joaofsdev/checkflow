$kind: http-request
url: "{{baseUrl}}/reservas/{{reservaId2}}/checkin"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 400', () => pm.response.to.have.status(400));
      pm.test('Mensagem de erro sobre quarto ocupado', () => {
          pm.expect(pm.response.json().error).to.include('ocupado');
      });
    language: text/javascript
order: 10000
